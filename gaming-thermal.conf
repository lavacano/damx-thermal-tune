# Frame cap at 165 FPS (monitor refresh rate) to reduce unnecessary GPU/CPU load
# DXVK games (Proton/Wine)
DXVK_FRAME_RATE=165
# Vulkan games via Mesa
MESA_VK_WSI_PRESENT_MODE=mailbox
# Force VSync via NVIDIA (prevents tearing + caps at refresh rate)
__GL_SYNC_TO_VBLANK=1
